'use strict';

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriverLicense = true;  // identify error in strict mode
if (hasDriversLicense) console.log('I can drive :)');

// Reserved words in strict mode
const interface = 'Audio';
// const private = 654;
// const if = 48;